Work in progress app for discovering new places and breaking routine.

To build locally: 
    download the repo, 
    CD into directory,
    run npm/yarn init,
    run node server.js
    open up localhost:3000