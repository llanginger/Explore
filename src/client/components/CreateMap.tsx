import * as React from "react"
import { CreateGoogleMap } from './TomMap';

export class PresentGoogleMap extends React.Component<any, any> {
  render() {
   return (
        <CreateGoogleMap />
    );
  }
}